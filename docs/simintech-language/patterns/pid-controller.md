# ПИД-регулятор

## Описание

ПИД-регулятор (пропорционально-интегрально-дифференциальный) — самый распространённый регулятор в промышленности. Используется для управления температурой, скоростью, положением и другими параметрами.

## Передаточная функция

```
W(s) = Kp + Ki/s + Kd*s
```

## Реализация в SimInTech

### Вариант 1: Блок «Язык программирования»

```c
// ПИД-регулятор с защитой от интегрального насыщения
const Kp = 1.5;     // пропорциональный коэффициент
const Ki = 0.8;     // интегральный коэффициент
const Kd = 0.3;     // дифференциальный коэффициент
const u_max = 10.0; // максимальное управление
const u_min = -10.0;// минимальное управление

var error, p_term, i_term, d_term, u_raw;
var prev_error = 0;
init integral = 0;

input setpoint;     // задание (уставка)
input measurement;  // измерение (с датчика)
output control;     // управляющий сигнал

// Вычисление ошибки
error = setpoint - measurement;

// П-составляющая
p_term = Kp * error;

// И-составляющая (трапецеидальное интегрирование)
integral = integral + (error + prev_error) * h / 2.0;
i_term = Ki * integral;

// Д-составляющая
d_term = Kd * (error - prev_error) / h;

// Суммарное управление
u_raw = p_term + i_term + d_term;

// Ограничение управления + anti-windup
if (u_raw > u_max) {
    control = u_max;
    integral = integral - (error + prev_error) * h / 2.0; // заморозка интеграла
} else if (u_raw < u_min) {
    control = u_min;
    integral = integral - (error + prev_error) * h / 2.0;
} else {
    control = u_raw;
}

prev_error = error;
```

### Вариант 2: Схема из блоков

1. **Блоки-источники**: Константа (setpoint), Константа (measurement)
2. **Сумматор** (setpoint − measurement) → ошибка
3. **Усилитель** (Kp) → P-ветвь
4. **Интегратор** (Ki/s) → I-ветвь
5. **Дифференциатор** (Kd·s) → D-ветвь
6. **Сумматор** (P + I + D) → управление

## Расчёт коэффициентов

### Метод Циглера-Никольса (колебательный)

1. Установить Ki = 0, Kd = 0
2. Увеличивать Kp до появления незатухающих колебаний → Ku (критический коэффициент)
3. Измерить период колебаний → Tu
4. Рассчитать:

| Тип | Kp | Ki | Kd |
|-----|----|----|-----|
| P | 0.5·Ku | — | — |
| PI | 0.45·Ku | 0.54·Ku/Tu | — |
| PID | 0.6·Ku | 1.2·Ku/Tu | 0.075·Ku·Tu |

## Типичные ошибки

1. **Интегральное насыщение (windup)** — интеграл растёт до бесконечности → anti-windup защита
2. **Шум в D-составляющей** — усиление шума датчика → фильтр НЧ на входе D-ветви
3. **Слишком большой шаг h** — интегрирование расходится → уменьшить шаг
4. **Знак ошибки** — перепутан setpoint и measurement → проверить полярность

## Тестирование

Подать на вход ступенчатый сигнал (блок «Ступенька»). Оценить:
- Время переходного процесса (до 5% от установившегося значения)
- Перерегулирование (%)
- Статическая ошибка (должна быть 0 при наличии I-составляющей)
