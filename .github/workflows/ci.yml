name: CI

# Проверяется всё, что не требует COM: COM доступен только на Windows с
# зарегистрированным `mmain.exe /regserver`, поэтому инструменты, трогающие
# SimInTech, здесь не запускаются и запускаться не могут. Тесты в `tests/unit`
# написаны так, чтобы обходиться без COM, и на Linux проходят целиком.
on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Пины зависимостей
        # До установки: мёртвый SHA (такое уже было — историю simintech-code
        # пересоздавали) падал бы невнятным `upload-pack: not our ref` изнутри
        # pip. Здесь он называется по имени, и починка очевидна.
        run: python scripts/check_pins.py

      - name: Установка
        run: pip install -e ".[test]" flake8 mypy

      - name: Стиль
        run: flake8 simintech_mcp tests scripts --max-line-length=88 --extend-ignore=E203,W503

      - name: Типы
        run: mypy

      - name: Тесты
        run: pytest tests/unit -q

  # Проверка секретов по всей истории.
  # `fetch-depth: 0` обязателен: при checkout по умолчанию виден один коммит,
  # и секрет, попавший в историю раньше, остался бы незамеченным.
  secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Установка gitleaks
        env:
          GITLEAKS_VERSION: "8.30.1"
          # Контрольная сумма из файла, который проект публикует вместе с
          # выпуском: скачанный архив — тот же, что выпустил проект, иначе шаг
          # падает до запуска проверок.
          GITLEAKS_SHA256: "551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb"
        run: |
          curl -sSfL -o gitleaks.tar.gz "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          echo "${GITLEAKS_SHA256}  gitleaks.tar.gz" | sha256sum -c -
          tar xzf gitleaks.tar.gz gitleaks

      # Секреты — по всей истории: из истории секрет не уходит сам.
      # `--exit-code 1` закреплён явно: у gitleaks он по умолчанию 1, но
      # проверка не должна зависеть от умолчания, которое может смениться.
      - name: Секреты (вся история)
        run: ./gitleaks git --exit-code 1 --no-banner --redact --log-opts="--all" --config .gitleaks.toml .

